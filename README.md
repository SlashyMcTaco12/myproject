כל הנתונים נשמרים במסד נתונים בענן, לא לשמור פרטים אישיים חשובים בבקשה

הפעלה:
1. פתיחת התיקייה הראשית והרצת הפעולות:
 A. 'npm i -g nodemon'
 B. 'npm i'
 C. 'npm start'
2. פתיחת תת-תיקייה 'server' והרצת הפקודה nodemon index
